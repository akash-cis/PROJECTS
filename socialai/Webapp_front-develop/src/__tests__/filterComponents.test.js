import React from "react"
import { render } from "@testing-library/react"
import { ManageFilters } from "../components/commons/manageFilters"

test.skip("Does", () => {
  const { getByTestId } = render(<ManageFilters />)
})
